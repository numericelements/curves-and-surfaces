import { expect } from 'chai';
import { RationalBSplineR1toR2 } from '../../../src/bsplines/R1toR2/RationalBSplineR1toR2';
import { RationalBSplineR1toR2DifferentialProperties } from '../../../src/bsplines/R1toR2/RationalBSplineR1toR2DifferentialProperties';
import { Vector3d } from '../../../src/mathVector/Vector3d';

describe('RationalBSplineR1toR2DifferentialProperties', () => {
    
    /*
    it('can be initialized with initializer', () => {

        const cp0 = new Vector3d(1, 0, 1)
        const cp1 = new Vector3d(1, 1, Math.pow(2, 0.5) / 2)
        const cp2 = new Vector3d(0, 1, 1)
    

        let spline = new RationalBSplineR1toR2([ cp0, cp1, cp2], [ 0, 0, 0, 1, 1, 1 ])
        spline.elevateDegree()
        const s = new RationalBSplineR1toR2DifferentialProperties(spline)
        let cn = s.curvatureNumerator()
        //console.log(spline.controlPoints[0])
        //console.log(cn)
    });
    */

});